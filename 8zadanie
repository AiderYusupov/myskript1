#!/bin/bash 

echo "Делаем 8 задание!"
echo "Перейдём в директорию /etc"
cd /etc && pwd
echo "Выводим все файлы из папки /etc в которых есть имя вашего пользователя, выводим сразу без ошибок!"
grep -i aider * 2>> /dev/null
echo "Вот и всё!"

